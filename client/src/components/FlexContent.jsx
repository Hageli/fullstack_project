import { Box } from '@mui/material';
import { styled } from  '@mui/system';

// FLEX COMPONENT TO STYLE OTHER ELEMENTS

const FlexContent = styled(Box)({
    display: 'flex',
    justifyContent: 'space-between',
    alignItems: 'center',
    });

    export default FlexContent;